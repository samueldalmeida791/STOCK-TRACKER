name: stock_tracker
description: Simple stock tracking app with watchlist, charts, and alerts
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter: 
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.2.3
  flutter_local_notifications: ^17.2.1
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
